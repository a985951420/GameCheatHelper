# 游戏秘籍快捷输入工具 - 系统设计文档

## 1. 系统架构设计

### 1.1 整体架构

本系统采用经典的三层架构 + MVVM 模式，确保代码的可维护性和可扩展性。

```
┌─────────────────────────────────────────────────────────┐
│                    Presentation Layer                    │
│                        (UI Layer)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   MainView   │  │  CheatView   │  │SettingsView  │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
│         │                 │                  │          │
│         ▼                 ▼                  ▼          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │MainViewModel │  │CheatViewModel│  │SettingsVM    │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
└─────────┼──────────────────┼──────────────────┼─────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────┐
│                     Business Layer                       │
│                    (Services Layer)                      │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │ GameDetection   │  │  HotKeyManager  │              │
│  │    Service      │  │                 │              │
│  └────────┬────────┘  └────────┬────────┘              │
│           │                    │                        │
│  ┌────────┴────────┐  ┌────────┴────────┐              │
│  │  CheatCode      │  │     Config      │              │
│  │    Service      │  │    Service      │              │
│  └─────────────────┘  └─────────────────┘              │
└─────────────────────────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────┐
│                       Core Layer                         │
│                   (Domain & Logic)                       │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │  CheatExecutor  │  │ KeyboardSim     │              │
│  └─────────────────┘  └─────────────────┘              │
│                                                          │
│  ┌──────────────────────────────────────┐              │
│  │            Data Models                │              │
│  │  GameInfo | CheatCode | HotKey       │              │
│  └──────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────┐
│                   Infrastructure Layer                   │
│                 (External Dependencies)                  │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │   Win32 API     │  │   File System   │              │
│  │   (User32.dll)  │  │   (JSON Config) │              │
│  └─────────────────┘  └─────────────────┘              │
└─────────────────────────────────────────────────────────┘
```

### 1.2 架构层次说明

#### 1.2.1 表示层 (Presentation Layer)
- **职责**: 用户界面展示和交互
- **技术**: WPF + MVVM 模式
- **组成**: Views (XAML) + ViewModels (C#)

#### 1.2.2 业务层 (Business Layer)
- **职责**: 业务逻辑和服务协调
- **技术**: C# 服务类
- **组成**: 各种业务服务

#### 1.2.3 核心层 (Core Layer)
- **职责**: 核心功能和领域模型
- **技术**: C# 类库
- **组成**: 数据模型、执行引擎、工具类

#### 1.2.4 基础设施层 (Infrastructure Layer)
- **职责**: 外部依赖和底层 API
- **技术**: P/Invoke、文件系统、日志框架

---

## 2. 核心模块设计

### 2.1 游戏检测模块

#### 检测流程

```
[开始] → [启动定时器] → [轮询进程列表]
                              ↓
                    [匹配游戏进程名称]
                              ↓
                    ┌─────────┴─────────┐
                    ↓                   ↓
            [找到游戏]              [未找到]
                    ↓                   ↓
          [触发GameDetected]      [触发GameLost]
                    ↓                   ↓
              [更新状态]            [清除状态]
```

### 2.2 热键管理模块

#### 热键注册流程

```
[用户配置热键]
      ↓
[HotKeyManager.RegisterHotKey()]
      ↓
[调用 Win32API.RegisterHotKey()]
      ↓
[注册成功?] ─No→ [返回失败，提示用户]
      ↓ Yes
[保存到内部字典]
      ↓
[返回成功]
```

### 2.3 秘籍执行模块

#### 执行流程

```
[接收秘籍执行请求]
      ↓
[检查游戏是否运行]
      ↓
[激活游戏窗口]
      ↓
[等待 100ms]
      ↓
[按下 Enter (打开聊天框)]
      ↓
[输入秘籍代码]
      ↓
[按下 Enter (确认)]
      ↓
[返回结果]
```

---

## 3. 数据模型设计

### 3.1 核心模型

#### GameType 枚举
```csharp
public enum GameType
{
    Unknown = 0,
    Warcraft3 = 1,
    StarCraft = 2
}
```

#### CheatCode 类
```csharp
public class CheatCode
{
    public string Id { get; set; }
    public GameType Game { get; set; }
    public string Code { get; set; }
    public string Description { get; set; }
    public bool Enabled { get; set; }
}
```

#### HotKey 类
```csharp
public class HotKey
{
    public int Id { get; set; }
    public Keys Key { get; set; }
    public uint Modifiers { get; set; }
    public string CheatCodeId { get; set; }
}
```

---

## 4. 界面设计

### 4.1 主窗口布局

```
┌──────────────────────────────────────────────────┐
│ GameCheatHelper                          [_][□][×]│
├──────────────────────────────────────────────────┤
│  🎮 当前游戏: 魔兽争霸3                   [⚙设置] │
├──────────────────────────────────────────────────┤
│  搜索: [_______________] [🔍]                     │
├──────────────────────────────────────────────────┤
│  秘籍列表                                         │
│  ┌────────┬──────────────┬──────────┬────────┐  │
│  │ 热键   │ 秘籍代码      │ 描述     │ 启用   │  │
│  ├────────┼──────────────┼──────────┼────────┤  │
│  │ F1     │ greedisgood  │ 金木+500 │ [✓]    │  │
│  │ F2     │ iseedeadpeople│ 显示全图│ [✓]    │  │
│  └────────┴──────────────┴──────────┴────────┘  │
├──────────────────────────────────────────────────┤
│  状态: 就绪                       v1.0.0          │
└──────────────────────────────────────────────────┘
```

---

## 5. 状态管理设计

### 5.1 应用程序状态机

```
     [启动]
        ↓
   [初始化]
        ↓
   [待机状态] ←→ [活动状态]
        ↓              ↓
   [执行秘籍]    [检测游戏]
```

---

## 6. 安全性设计

### 6.1 安全原则
1. 最小权限原则
2. 只读游戏
3. 官方功能
4. 透明操作

### 6.2 输入验证
- 秘籍代码长度限制: 1-50 字符
- 仅允许字母、数字、空格
- 热键冲突检测

---

## 7. 性能优化

### 7.1 优化策略
- 游戏检测间隔: 2000ms
- 输入延迟: 10-50ms 可配置
- 使用虚拟化列表
- 异步操作避免 UI 卡顿

---

## 8. 可扩展性设计

### 8.1 新游戏支持
通过实现 IGameDetector 接口轻松添加新游戏支持

### 8.2 插件系统 (未来)
支持第三方插件扩展功能

---

**文档版本**: v1.0
**创建日期**: 2026-01-28
**最后更新**: 2026-01-28
