<Window x:Class="GameCheatHelper.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="游戏秘籍助手 v1.0.0 - by Time"
        Height="600"
        Width="800"
        MinHeight="500"
        MinWidth="700"
        WindowStartupLocation="CenterScreen"
        FontFamily="{StaticResource DefaultFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部状态栏 -->
        <Border Grid.Row="0"
                Background="{StaticResource PrimaryBrush}"
                Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 游戏选择 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="🎮 游戏: "
                               Foreground="White"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="0,0,5,0"/>
                    <ComboBox x:Name="GameTypeComboBox"
                              ItemsSource="{Binding GameTypes}"
                              SelectedItem="{Binding SelectedGameType}"
                              DisplayMemberPath="Value"
                              SelectedValuePath="Key"
                              Width="150"
                              Height="28"
                              FontSize="13"
                              VerticalAlignment="Center"
                              SelectionChanged="GameTypeComboBox_SelectionChanged"/>
                </StackPanel>

                <!-- 游戏检测状态 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="📡 检测状态: "
                               Foreground="White"
                               FontSize="14"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding GameStatus}"
                               Foreground="White"
                               FontSize="14"
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Content="⚙ 设置"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="1"
                        BorderBrush="White"
                        Padding="10,5"
                        Cursor="Hand"
                        Click="SettingsButton_Click"/>
            </Grid>
        </Border>

        <!-- 主内容区 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 搜索栏 -->
            <Border Grid.Row="0"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    Padding="10"
                    Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchTextBox"
                             Grid.Column="0"
                             Padding="5"
                             FontSize="13"
                             VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="🔍 搜索"
                            Margin="10,0,0,0"
                            Padding="15,5"
                            Click="SearchButton_Click"/>
                </Grid>
            </Border>

            <!-- 秘籍列表 -->
            <Border Grid.Row="1"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1">
                <DataGrid x:Name="CheatDataGrid"
                          ItemsSource="{Binding CheatCodes}"
                          SelectedItem="{Binding SelectedCheat}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F5F5F5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="热键" Binding="{Binding HotKeyText}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="秘籍代码" Binding="{Binding Code}" Width="200" IsReadOnly="True"/>
                        <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="分类" Binding="{Binding Category}" Width="100" IsReadOnly="True"/>
                        <DataGridCheckBoxColumn Header="启用" Binding="{Binding Enabled}" Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- 操作按钮 -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Margin="0,10,0,0">
                <Button Content="➕ 添加秘籍"
                        Padding="10,5"
                        Margin="0,0,10,0"
                        Click="AddCheatButton_Click"/>
                <Button Content="✏ 编辑"
                        Padding="10,5"
                        Margin="0,0,10,0"
                        Click="EditCheatButton_Click"/>
                <Button Content="🗑 删除"
                        Padding="10,5"
                        Margin="0,0,10,0"
                        Click="DeleteCheatButton_Click"/>

                <!-- 星际争霸1 解除人口上限按钮 -->
                <Border Margin="10,0,0,0"
                        BorderBrush="#FF5722"
                        BorderThickness="1"
                        CornerRadius="3">
                    <Button Content="{Binding SupplyCapButtonText}"
                            Padding="12,5"
                            FontWeight="Bold"
                            Background="#FFF3E0"
                            Foreground="#E65100"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="通过内存修改解除星际争霸1的200人口上限，多人模式下只给自己解除（需管理员权限）"
                            Command="{Binding ToggleSupplyCapCommand}"/>
                </Border>

                <!-- 星际争霸1 加钱按钮 -->
                <Border Margin="6,0,0,0"
                        BorderBrush="#4CAF50"
                        BorderThickness="1"
                        CornerRadius="3">
                    <Button Content="💰 加钱(+10000)"
                            Padding="12,5"
                            FontWeight="Bold"
                            Background="#E8F5E9"
                            Foreground="#2E7D32"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="一次性给自己增加 10000水晶矿 + 10000瓦斯（多人模式可用，需管理员权限）"
                            Command="{Binding AddResourcesCommand}"/>
                </Border>

                <!-- 星际争霸1 持续加钱按钮 -->
                <Border Margin="6,0,0,0"
                        BorderBrush="#FF9800"
                        BorderThickness="1"
                        CornerRadius="3">
                    <Button Padding="12,5"
                            FontWeight="Bold"
                            Background="#FFF8E1"
                            Foreground="#E65100"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="每5秒自动给自己增加 10000水晶矿 + 10000瓦斯，再次点击停止"
                            Command="{Binding ToggleResourceBoostCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Content" Value="🔄 持续加钱"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsResourceBoostActive}" Value="True">
                                        <Setter Property="Content" Value="⏹ 停止加钱"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Border>

                <!-- 星际争霸1 建造加速按钮 -->
                <Border Margin="6,0,0,0"
                        BorderBrush="#9C27B0"
                        BorderThickness="1"
                        CornerRadius="3">
                    <Button Padding="12,5"
                            FontWeight="Bold"
                            Background="#F3E5F5"
                            Foreground="#6A1B9A"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="启动后建造/训练几乎瞬间完成（只影响自己，多人模式可用）"
                            Command="{Binding ToggleBuildSpeedBoostCommand}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Content" Value="⚡ 建造加速"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBuildSpeedBoostActive}" Value="True">
                                        <Setter Property="Content" Value="⏹ 停止加速"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Border>

                <!-- 调试：扫描玩家索引按钮 -->
                <Border Margin="6,0,0,0"
                        BorderBrush="#607D8B"
                        BorderThickness="1"
                        CornerRadius="3">
                    <Button Content="🔍 扫描玩家索引"
                            Padding="12,5"
                            FontWeight="Bold"
                            Background="#ECEFF1"
                            Foreground="#37474F"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="扫描所有玩家槽位的资源值，用于多人模式调试（查看日志文件）"
                            Command="{Binding ScanPlayerIndexCommand}"/>
                </Border>
            </StackPanel>
        </Grid>

        <!-- 底部状态栏 -->
        <Border Grid.Row="2"
                Background="#F5F5F5"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0"
                Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusMessage}"
                           VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Foreground="#757575"
                           Margin="0,0,15,0">
                    <Hyperlink NavigateUri="https://github.com/a985951420/GameCheatHelper"
                               RequestNavigate="Hyperlink_RequestNavigate"
                               TextDecorations="None"
                               Foreground="#2196F3">
                        <Run Text="GitHub: a985951420/GameCheatHelper"/>
                    </Hyperlink>
                </TextBlock>

                <TextBlock Grid.Column="2"
                           Text="v1.0.0 | 开发：Time"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Foreground="#757575"/>
            </Grid>
        </Border>
    </Grid>
</Window>
